slam_toolbox:
  ros__parameters:
    use_sim_time: false

    # Frames
    odom_frame: odom
    map_frame: map
    base_frame: base_link
    scan_topic: /scan_reliable   # NIE /scan! p2l publikuje /scan best_effort, slam chce
                                  # reliable → QoS incompatible → slam 0 skanów → brak mapy.
                                  # scan_qos_relay daje /scan_reliable (reliable). slam czyta
                                  # scan_topic z TEGO yaml (override z launcha nie zadziałał).
    mode: mapping        # 'mapping' tworzy mapę; 'localization' używa zapisanej

    # Solver
    solver_plugin: solver_plugins::CeresSolver
    ceres_linear_solver: SPARSE_NORMAL_CHOLESKY
    ceres_preconditioner: SCHUR_JACOBI
    ceres_trust_strategy: LEVENBERG_MARQUARDT
    ceres_dogleg_type: TRADITIONAL_DOGLEG
    ceres_loss_function: None

    # Map params
    resolution: 0.05
    max_laser_range: 20.0    # Mid-360 sięga 40 m; 20 wystarcza dla typowego labu i
                              # gives connectivity między mapami localymi w dłuższym
                              # korytarzu. Pasuje też range_max=25.0 w pointcloud_to_laserscan.
    minimum_time_interval: 0.2     # 5 Hz max update — G1 chodzi wolno, częstsze update
                                    # daje więcej constraintów do scan matchera
    minimum_travel_distance: 0.05  # 5 cm — z minimum_velocity_floor 0.12 m/s = update ~0.4s
                                    # (poprzednio 0.2 m = update co ~1.7s, mapa fragmentaryczna)
    minimum_travel_heading: 0.05   # ~3°
    transform_publish_period: 0.05
    map_update_interval: 1.0
    transform_timeout: 0.5
    tf_buffer_duration: 30.0
    stack_size_to_use: 40000000

    # Scan matcher
    use_scan_matching: true
    use_scan_barycenter: true
    scan_buffer_size: 30
    scan_buffer_maximum_scan_distance: 10.0
    link_match_minimum_response_fine: 0.1
    link_scan_maximum_distance: 1.5

    # Correlation search (skanowy)
    correlation_search_space_dimension: 0.5
    correlation_search_space_resolution: 0.01
    correlation_search_space_smear_deviation: 0.1

    # Loop closure
    do_loop_closing: true
    loop_search_maximum_distance: 6.0
    loop_match_minimum_chain_size: 8
    loop_match_maximum_variance_coarse: 3.0
    loop_match_minimum_response_coarse: 0.35
    loop_match_minimum_response_fine: 0.45
    loop_search_space_dimension: 8.0
    loop_search_space_resolution: 0.05
    loop_search_space_smear_deviation: 0.03

    # Scan matcher params (precision)
    distance_variance_penalty: 0.5
    angle_variance_penalty: 1.0
    fine_search_angle_offset: 0.00349
    coarse_search_angle_offset: 0.349
    coarse_angle_resolution: 0.0349
    minimum_angle_penalty: 0.9
    minimum_distance_penalty: 0.5
    use_response_expansion: true
