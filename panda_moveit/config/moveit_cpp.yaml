# Konfiguracja MoveItPy (W8). Node z moveit_py sam ładuje plannery i scenę —
# ten plik mówi mu, jak zbudować planning scene monitor i który pipeline włączyć.
# Na Jazzy MoveItCpp czyta stąd tylko name, robot_description i
# wait_for_initial_state_timeout. Topici są stałe: stan przegubów z /joint_states,
# a scenę (stół, klocek, attach) node publikuje na /monitored_planning_scene -
# ten topic ustaw w RViz w displayu PlanningScene.
planning_scene_monitor_options:
  name: "planning_scene_monitor"
  robot_description: "robot_description"
  wait_for_initial_state_timeout: 10.0

planning_pipelines:
  pipeline_names: ["ompl"]

plan_request_params:
  planning_attempts: 3
  planning_pipeline: ompl
  planner_id: "RRTConnectkConfigDefault"
  max_velocity_scaling_factor: 0.5
  max_acceleration_scaling_factor: 0.5
